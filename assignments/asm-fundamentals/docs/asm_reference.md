# Ассемблер Intel x86-64

Примечание: Справочник содержит избыточные варианты (MUL/IMUL, DIV/IDIV, JL/JB, etc.). Выбор конкретной инструкции и размера регистра принимается программистом на основе задачи. Ошибки выбора (например, DIV без обнуления RDX, MOVZX вместо MOVSX для знаковых данных) проявляются при выполнении.

Тех. ограничения: только Linux x86-64 ABI, синтаксис NASM, без SIMD/расширений.
Ссылка на спеку: Intel SDM Vol. 1 §3.4 "General-Purpose Registers" - https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html; NASM Manual §3.1 "Basic Syntax" - https://www.nasm.us/doc/nasmdoc3.html


---


## Регистры

### Регистры общего назначения

| 64-бит | 32-бит | 16-бит | 8-бит (low) | 8-бит (high) | Назначение по ABI             |
|--------|--------|--------|-------------|--------------|-------------------------------|
| RAX    | EAX    | AX     | AL          | AH           | аккумулятор                   |
| RBX    | EBX    | BX     | BL          | BH           | базовый                       |
| RCX    | ECX    | CX     | CL          | CH           | счетчик loop                  |
| RDX    | EDX    | DX     | DL          | DH           | данные                        |
| RSI    | ESI    | SI     | SIL         | -            | источник                      |
| RDI    | EDI    | DI     | DIL         | -            | приемник                      |
| RSP    | ESP    | SP     | SPL         | -            | указатель на вершину стека    |
| RBP    | EBP    | BP     | BPL         | -            | указатель на базу кадра стека |
| R8     | R8D    | R8W    | R8B         | -            | 4-й аргумент syscall          |
| R9     | R9D    | R9W    | R9B         | -            | 5-й аргумент syscall          |
| R10    | R10D   | R10W   | R10B        | -            | 6-й аргумент syscall          |
| R11    | R11D   | R11W   | R11B        | -            | Временный (clobbered syscall) |
| R12    | R12D   | R12W   | R12B        | -            | Сохраняемый (callee-saved)    |
| R13    | R13D   | R13W   | R13B        | -            | Сохраняемый (callee-saved)    |
| R14    | R14D   | R14W   | R14B        | -            | Сохраняемый (callee-saved)    |
| R15    | R15D   | R15W   | R15B        | -            | Сохраняемый (callee-saved)    |

Примечание: Запись в 32-битный регистр (EAX) обнуляет старшие 32 бита 64-битного (RAX). Запись в 8-битный (AL) затрагивает только младшие 8 бит.

### Сегментные регистры

| Мнемоника | Имя          | Назначение по ABI                    |
|-----------|--------------|--------------------------------------|
| CS        | Code Segment | Адрес сегмента с машинными командами |


---

## Инструкции

### Пересылка данных

| Мнемоника | Формат операндов | Описание                            | Размер результата |
|-----------|------------------|-------------------------------------|-------------------|
| MOV       | reg, reg/mem/imm | Копирование                         | Без изменений     |
| MOVZX     | reg, reg/mem     | Копирование с нулевым расширением   | До размера dest   |
| MOVSX     | reg, reg/mem     | Копирование со знаковым расширением | До размера dest   |
| LEA       | reg, mem         | Загрузка эффективного адреса        | 64 бита           |
| XCHG      | reg, reg         | Обмен значениями                    | Без изменений     |
| PUSH      | reg/mem/imm      | Поместить в стек                    | 64 бита           |
| POP       | reg/mem          | Извлечь из стека                    | 64 бита           |

Варианты размера операнда: byte (8), word (16), dword (32), qword (64). Указывается явно: `mov byte [addr], 1`.


### Правило записи в под-регистры

| Запись         | Влияние на RAX       | Безопасно для         |
|----------------|----------------------|-----------------------|
| `mov al, imm`  | Биты 0-7             | 8-битные операции     |
| `mov ax, imm`  | Биты 0-15            | 16-битные операции    |
| `mov eax, imm` | Биты 0-31, 32-63 = 0 | 32/64-битные операции |
| `mov rax, imm` | Биты 0-63            | 64-битные операции    |

Важно: Только запись в EAX гарантированно обнуляет старшие 32 бита RAX

---

### Арифметические

| Мнемоника | Формат операндов     | Описание                                    | Флаги              |
|-----------|----------------------|---------------------------------------------|--------------------|
| ADD       | reg/mem, reg/mem/imm | Сложение                                    | CF, ZF, SF, OF     |
| SUB       | reg/mem, reg/mem/imm | Вычитание                                   | CF, ZF, SF, OF     |
| INC       | reg/mem              | Инкремент на 1                              | ZF, SF, OF (не CF) |
| DEC       | reg/mem              | Декремент на 1                              | ZF, SF, OF (не CF) |
| NEG       | reg/mem              | Арифметическое отрицание                    | CF, ZF, SF, OF     |
| IMUL      | reg, reg/mem/imm     | Знаковое умножение (результат в RDX:RAX)    | CF, OF             |
| MUL       | reg/mem              | Беззнаковое умножение (результат в RDX:RAX) | CF, OF             |
| IDIV      | reg/mem              | Знаковое деление (результат в RDX:RAX)      | -                  |
| DIV       | reg/mem              | Беззнаковое деление (результат в RDX:RAX)   | -                  |
| CDQ       | -                    | Расширение EAX в EDX:EAX                    | -                  |
| CQO       | -                    | Расширение RAX в RDX:RAX                    | -                  |

Примечание: 
MUL/IMUL с одним операндом используют RAX как аккумулятор, результат в RDX:RAX (128 бит). 
DIV/IDIV делят RDX:RAX на операнд, частное в RAX, остаток в RDX.

---

### Сравнение

| Мнемоника | Формат операндов     | Описание                | Изменяемые флаги       |
|-----------|----------------------|-------------------------|------------------------|
| CMP       | reg/mem, reg/mem/imm | Вычитание без записи    | CF, OF, SF, ZF, AF, PF |
| TEST      | reg, reg/mem/imm     | Логическое И без записи | ZF, SF, PF             |

### Переходы
| Мнемоника | Формат операндов | Описание                                 | Флаги        |
|-----------|------------------|------------------------------------------|--------------|
| JMP       | label            | Безусловный переход                      | -            |
| JE/JZ     | label            | равно/ноль                               | ZF=1         |
| JNE/JNZ   | label            | не равно/не ноль                         | ZF=0         |
| JL/JNGE   | label            | меньше (знаковое)                        | SF≠OF        |
| JLE/JNG   | label            | меньше или равно                         | ZF=1 ∨ SF≠OF |
| JG/JNLE   | label            | больше (знаковое)                        | ZF=0 ∧ SF=OF |
| JGE/JNL   | label            | больше или равно                         | SF=OF        |
| JB/JNAE   | label            | ниже (беззнаковое)                       | CF=1         |
| JBE/JNA   | label            | ниже или равно                           | CF=1 ∨ ZF=1  |
| JA/JNBE   | label            | выше (беззнаковое)                       | CF=0 ∧ ZF=0  |
| JAE/JNB   | label            | выше или равно                           | CF=0         |
| LOOP      | label            | Цикл по RCX (RCX=RCX-1, если ≠0 переход) | -            |

---

### Логические

| Мнемоника | Формат операндов     | Описание                      | Флаги          |
|-----------|----------------------|-------------------------------|----------------|
| AND       | reg/mem, reg/mem/imm | Побитовое И                   | CF, ZF, SF, OF |
| OR        | reg/mem, reg/mem/imm | Побитовое ИЛИ                 | CF, ZF, SF, OF |
| XOR       | reg/mem, reg/mem/imm | Побитовое исключающее ИЛИ     | CF, ZF, SF, OF |
| NOT       | reg/mem              | Побитовое отрицание           | -              |
| SHL/SAL   | reg/mem, imm/CL      | Сдвиг влево                   | CF, ZF, SF, OF |
| SHR       | reg/mem, imm/CL      | Сдвиг вправо (логический)     | CF, ZF, SF, OF |
| SAR       | reg/mem, imm/CL      | Сдвиг вправо (арифметический) | CF, ZF, SF, OF |
| ROL       | reg/mem, imm/CL      | Циклический сдвиг влево       | CF, OF         |
| ROR       | reg/mem, imm/CL      | Циклический сдвиг вправо      | CF, OF         |

---

## Системные вызовы 

| Имя        | Действие                    | Номер | RDI    | RSI | RDX   | Изменение RAX |
|------------|-----------------------------|-------|--------|-----|-------|---------------|
| exit       | завершение процесса         | 60    | status | -   | -     | -             |
| exit_group | завершение процесса         | 231   | status | -   | -     | -             |
| read       | чтение RDX элементов из RSI | 0     | fd     | buf | count | count         |
| write      | запись RDX элементов в RSI  | 1     | fd     | buf | count | count         |

`count` - число считанных байт
`exit_group`: Exit code = 1 байт (0-255)
`syscall`:
- RCX/R11 перезаписываются ядром
- RAX должен содержать полный номер syscall - 64 бита

##  Файловые дескрипторы

| Код | Имя    | Назначение               | Пример использования       |
|-----|--------|--------------------------|----------------------------|
| 0   | stdin  | Стандартный ввод         | `mov rdi, 0` перед `read`  |
| 1   | stdout | Стандартный вывод        | `mov rdi, 1` перед `write` |
| 2   | stderr | Стандартный вывод ошибок | `mov rdi, 2` перед `write` |

Пример:
```nasm
; Чтение из stdin
mov rax, 0          ; sys_read
mov rdi, 0          ; fd = stdin
mov rsi, buf        ; буфер
mov rdx, 10         ; количество байт
syscall

; Запись в stdout
mov rax, 1          ; sys_write
mov rdi, 1          ; fd = stdout
mov rsi, buf        ; буфер
mov rdx, 10         ; количество байт
syscall
```

Примечание:
- Файловые дескрипторы возвращаются syscall `open` (номер 2), `socket` (номер 41)
- Закрытие: `close` (номер 3), аргумент в RDI
- Ошибка: возврат -1 (0xFFFFFFFFFFFFFFFF), код ошибки в errno


---

## Директивы NASM
### Секции

| Директива | Описание                    | Выравнивание | Инициализация          |
|-----------|-----------------------------|--------------|------------------------|
| .text     | Код программы               | 16 байт      | -                      |
| .data     | Инициализированные данные   | 1 байт       | Явная (db, dw, dd, dq) |
| .bss      | Неинициализированные данные | 16 байт      | Нули (ядро)            |
| .rodata   | Только для чтения           | 16 байт      | Явная                  |

### Данные

.bss: ядро инициализирует нулями при загрузке, не занимает места на диске

| Команда резервирования | Мнемоника типа | Тип   | Описание        | Размер элемента |
|------------------------|----------------|-------|-----------------|-----------------|
| resb                   | db             | byte  | Байт            | 1 байт          |
| resw                   | dw             | word  | Слово           | 2 байт          |
| resd                   | dd             | dword | Двойное слово   | 4 байт          |
| resq                   | dq             | qword | Четверное слово | 8 байт          |

`name resd 1`: выделить 4 байта (1 double word) по адресу с идентификатором name


---

## Флаги - eflags

Бит 0: CF (Carry Flag) — перенос при сложении/сдвиге
Бит 1: всегда установлен в 1. Биты 12-14, 15, 17-18, 32-63 зарезервированы процессором.
Бит 2: PF (Parity Flag) — чётность младшего байта результата
Бит 4: AF (Adjust Flag) — вспомогательный перенос (BCD-арифметика)
Бит 6: ZF (Zero Flag) — результат операции равен нулю
Бит 7: SF (Sign Flag) — старший бит результата (знак)
Бит 8: TF (Trap Flag) — пошаговая трассировка
Бит 9: IF (Interrupt Flag) — разрешение аппаратных прерываний
Бит 10: DF (Direction Flag) — направление строковых операций
Бит 11: OF (Overflow Flag) — знаковое переполнение

---

## Массивы

Массив не является примитивом ISA x86-64, ABI или синтаксиса NASM. 
Это паттерн использования памяти, составленный из: 
(1) директив размещения данных, 
(2) режимов адресации, 
(3) дисциплины программиста.

Массив - композиция элементов:
1. Размещение в памяти
2. Доступ по индексу
3. Границы и размер

```asm
; 1. Объявление (NASM §3.2)
section .data
array dd 10, 20, 30, 40, 50   ; 5 элементов, 4 байта каждый

; 2. Получение базового адреса (NASM §3.7)
lea rsi, [array]              ; rsi = &array[0]

; 3. Доступ к элементу (Intel SDM Vol. 1 §3.4)
mov ecx, 2                    ; индекс
mov eax, [rsi + rcx*4]        ; eax = array[2]

; 4. Проверка границ
cmp ecx, 5
jge .out_of_bounds

; 5. Передача в функцию (System V ABI §3.2.3)
mov rdi, rsi                  ; 1-й аргумент = указатель на массив
mov rsi, 5                    ; 2-й аргумент = длина
call process_array
```

---

## Функции

### Базовый уровень - без call/ret - Метки как функции

| Компонент     | Инструкция    | Назначение             |
|---------------|---------------|------------------------|
| Вход          | `jmp .label`  | Переход к коду функции |
| Аргументы     | RDI, RSI, RDX | Передача значений      |
| Возврат       | RAX           | Результат функции      |
| Возврат в код | `jmp .return` | Переход обратно        |

Пример:
```nasm
section .text
global _start
_start:
    mov rdi, 42          ; аргумент
    jmp .double          ; вызов
    
.double:
    shl rdi, 1           ; тело
    mov rax, rdi         ; результат
    jmp .return          ; возврат
    
.return:
    ; RAX = 84
    mov rdi, rax
    mov rax, 231
    syscall
```

---

# Отладка программы

## Значения и представления

| Операция                | Bash                                       | Python                        | Результат     |
|-------------------------|--------------------------------------------|-------------------------------|---------------|
| Сложение                | `printf "0x%x\n" $((0x401000 + 32))`       | `hex(0x401000 + 32)`          | `0x401020`    |
| Вычитание               | `printf "0x%x\n" $((0x401020 - 0x401000))` | `hex(0x401020 - 0x401000)`    | `0x20`        |
| Умножение               | `printf "0x%x\n" $((0x2a * 0x02))`         | `hex(0x2a * 0x02)`            | `0x54`        |
| Маскирование            | `printf "0x%x\n" $((0x6869 & 0xFF))`       | `hex(0x6869 & 0xFF)`          | `0x69`        |
| Сдвиг влево             | `printf "0x%x\n" $((97 << 8))`             | `hex(97 << 8)`                | `0x6100`      |
| Сдвиг вправо            | `printf "0x%x\n" $((0x6869 >> 8))`         | `hex(0x6869 >> 8)`            | `0x68`        |
| Побитовое И             | `printf "0x%x\n" $((0xFF & 0x0F))`         | `hex(0xFF & 0x0F)`            | `0x0F`        |
| Побитовое ИЛИ           | `printf "0x%x\n" $((0xF0 \| 0x0F))`        | `hex(0xF0 \| 0x0F)`           | `0xFF`        |
| Побитовое XOR           | `printf "0x%x\n" $((0xFF ^ 0x0F))`         | `hex(0xFF ^ 0x0F)`            | `0xF0`        |
| Инверсия                | `printf "0x%x\n" $((~0xFF & 0xFF))`        | `hex(~0xFF & 0xFF)`           | `0x00`        |
| Выравнивание по 16 байт | `printf "0x%x\n" $((0x401002 & ~0xF))`     | `hex(0x401002 & ~0xF)`        | `0x401000`    |
| Проверка бита N         | `printf "%d\n" $((val & (1 << N)))`        | `val & (1 << N)`              | `0` или `2^N` |
| Извлечение байта N      | `printf "0x%x\n" $((val >> (N*8)) & 0xFF)` | `hex((val >> (N*8)) & 0xFF)`  | `0xXX`        |
| ASCII->int              | `printf '%d' "'a"`                         | `ord('a')`                    | `97`          |
| int->ASCII              | `printf "\\$(printf '%03o' 97)"`           | `chr(97)`                     | `'a'`         |
| int->bytes (1 байт)     | -                                          | `(97).to_bytes(1, 'big')`     | `b'a'`        |
| bytes->int              | -                                          | `int.from_bytes(b'a', 'big')` | `97`          |

## Сборка

```shell
nasm -f elf64 -g src/step4_task.asm -o step4_task.o
ld -no-pie -m elf_x86_64 step4_task.o -o step4_task
```
---

## Проверка выравнивания
```text
readelf -W -S step4_task | grep -E '\.data|\.bss'
# [ 2] .data  PROGBITS  0000000000402000  002000  00000c  0  WA  0   0  1
# [ 3] .bss   NOBITS    000000000040200c  00200c  000010  0  WA  0   0  1
#                                                ↑ выравнивание = 1 байт
```

---

## GDB

Напечатать адреса строк - для точек останова
```shell
objdump -d step4_task
```

Ввод из пайплайна
Поставить останов после вызова sys_read
```text
gdb -q ./step4_task
Reading symbols from ./step4_task...
(gdb) b *0x401065
Breakpoint 1 at 0x401065: file src/step4_task.asm, line 139.
(gdb) r < <(echo -e "42\n2")
```

| Задача                               | Команда               | Пример вывода                                                   |
|--------------------------------------|-----------------------|-----------------------------------------------------------------|
| Запуск                               | `gdb -q ./step4_task` | `Reading symbols from ./step4_task`                             |
| Добавить точку останова по метке     | `b _start`            | `Breakpoint 1 at 0x401000`                                      |
| Добавить точку останова по адресу    | `b *0x401010`         | `Breakpoint 2 at 0x401010`                                      |
| Удалить точку останова по адресу     | `b *0x401010`         | `Breakpoint 2 at 0x401010`                                      |
| Все точки останова                   | `i b`                 | `Num     Type           Disp Enb Address            What`       |
| Удалить точку останова по номеру Num | `d 3` (Num=3)         |                                                                 |
| Продолжить выполнение                | `c`                   |                                                                 |
| Все регистры                         | `i r`                 |                                                                 |
| Все символы                          | `i variables`         | `0x0000000000402010  input_buf`                                 |
| Значение в 16-ричном                 | `p/x $rax`            | `$1 = 0x1`                                                      |
| Значение в 10-ричном                 | `p/d $rax`            | `$2 = 1`                                                        |
| Бинарное представление               | `p/t $rax`            | `$3 = 00000001`                                                 |
| Флаги                                | `p/t $eflags`         | `$5 = 1000000110`                                               |
| Авто-дизассемблер                    | `display/i $pc`       | `=> 0x401000: mov rax, 17`                                      |
| Шаг на строго одну инструкцию        | `si`                  | -                                                               |
| Адрес переменной                     | `p &input_buf`        | `$1 = (char *) 0x40200c`                                        |
| Адрес переменной (инфо о символе)    | `i address input_buf` | `Symbol "input_buf" is at 0x402010 ...`                         |
| Инфа о символе по адресу символа     | `i symbol 0x401018`   | `_start + 24 in section .text of ...`                           |
| Значение переменной                  | `x/dw 0x402010`       | `0x402010 <num1>:	42`                                           |
| Объект по адресу - как код           | `x/5i 0x401018`       | `0x401018 <_start+24>:	cmp    rax,0x0`                          |
| Объект по адресу - как данные        | `x/10xb 0x401018`     | `0x401018 <_start+24>:	0x48	0x83	0xf8	0x00	0x7e	0x71	0x48	0x8d` |
| Объект по адресу - как строка        | `x/20cb 0x401018`     | `0x401018 <_start+24>:	72 'H'	-125 '\203'	-8 '\370'`            |

Форматы команды `printf` (GDB и Bash)

| Specifier | Значение            | Тип    | GDB `printf`           | Bash `printf`            |
|-----------|---------------------|--------|------------------------|--------------------------|
| `%d`      | decimal (signed)    | int    | `printf "%d\n", 42`    | `printf '%d\n' 42`       |
| `%u`      | decimal (unsigned)  | uint   | `printf "%u\n", 42`    | `printf '%u\n' 42`       |
| `%x`      | hex (lowercase)     | int    | `printf "%x\n", 42`    | `printf '%x\n' 42`       |
| `%X`      | hex (uppercase)     | int    | `printf "%X\n", 42`    | `printf '%X\n' 42`       |
| `%c`      | символ (char)       | char   | `printf "%c\n", 65`    | `printf '%c\n' 65`       |
| `%s`      | строка              | char*  | `printf "%s\n", "hi"`  | `printf '%s\n' "hi"`     |
| `%f`      | float (decimal)     | double | `printf "%f\n", 3.14`  | `printf '%f\n' 3.14`     |
| `%e`      | float (exponential) | double | `printf "%e\n", 3.14`  | `printf '%e\n' 3.14`     |
| `%p`      | указатель (адрес)   | void*  | `printf "%p\n", &var`  | `printf '%p\n' "$var"`   |
| `%lld`    | long long decimal   | int64  | `printf "%lld\n", val` | `printf '%lld\n' "$val"` |
| `%llx`    | long long hex       | int64  | `printf "%llx\n", val` | `printf '%llx\n' "$val"` |
| `%o`      | octal               | int    | `printf "%o\n", 64`    | `printf '%o\n' 64`       |
| `%%`      | символ `%`          | -      | `printf "%%"`          | `printf '%%'`            |

Модификаторы ширины и точности:

| Модификатор | Значение                        | Пример                   | Вывод      |
|-------------|---------------------------------|--------------------------|------------|
| `%5d`       | мин. ширина 5                   | `printf "%5d", 42`       | `   42`    |
| `%05d`      | ширина 5, ведущие нули          | `printf "%05d", 42`      | `00042`    |
| `%-5d`      | ширина 5, выравнивание влево    | `printf "%-5d", 42`      | `42   `    |
| `%.2f`      | 2 знака после запятой           | `printf "%.2f", 3.14159` | `3.14`     |
| `%8.2f`     | ширина 8, 2 знака после запятой | `printf "%8.2f", 3.14`   | `    3.14` |
| `%#x`       | hex с префиксом 0x              | `printf "%#x", 42`       | `0x2a`     |