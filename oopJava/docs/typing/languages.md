# Типизация

---

# Понятие

---

## динамическая и утиная типизации

- язык с динамической типизацией может не поддерживать утиную


- утиная типизация может сочетаться со статической
    - TypeScript, Go

```python
import uuid


class FinancialOperationsDraft.Account:
    """Счет."""

    def __init__(self, balance: int, account_id: str):
        self._balance = balance
        self._stash = 0
        self._account_id = account_id

    def __str__(self):
        return f'счет №{self._account_id}'

    def __repr__(self):
        return f'баланс: {self._balance}\nзаначка: {self._stash}\n'


class SilentAccount:
    """Счет без печати.

    print вызывает метод acc_1.__str__

    если метод __str__ не реализован, print вызывает acc_1.__repr__

    если __repr__ не реализован, будет возвращено имя объекта функции и его адрес в памяти, например
        <__main__.SilentAccount object at 0x7987a0a97850>
    """

    def __init__(self, balance: int, account_id: str):
        self._balance = balance
        self._stash = 0
        self._account_id = account_id

    def __str__(self):
        """имитация ошибки

        без этой заглушки print вызовет __repr__ по умолчанию, который вернет имя объекта функции и его адрес в памяти
        """
        raise NotImplementedError('печать не поддерживается')

    def __repr__(self):
        """имитация ошибки

        без этой заглушки print вызовет __repr__ по умолчанию, который вернет имя объекта функции и его адрес в памяти
        """
        raise NotImplementedError('печать не поддерживается')


if __name__ == '__main__':
    # динамическая типизация
    x = 1  # int
    x = 'hello'  # str
    x.upper()  # метод класса str, при статической типизации такой код не был бы скомпилирован, так как первое переменная иницилизирована для типа int

    # утиная типизация
    acc_1 = FinancialOperationsDraft.Account(100, str(uuid.uuid4()))
    acc_2 = SilentAccount(100, str(uuid.uuid4()))

    # если метод для print реализован, выполнить его
    print(x)  # hello

    print(acc_1)  # счет №985ce6f1-1119-41c3-b94a-1ec5c7d56854
    # вызов функции print не удался, так как acc_2 не реализует __str__ и __repr__
    print(acc_2)  # NotImplementedError: печать не поддерживается
```

---

## elm и Haskell - обработка null/nil/Nothing

Elm — это чистый функциональный язык. Он не имеет null. Вместо этого используется тип Maybe a, который может быть:
Just value — значение есть.
Nothing — значение отсутствует.
Nothing — это не ошибка, а встроенный тип.
Maybe — это всеобъемлющий способ обработки отсутствия значений.
null-ошибки невозможны на уровне типа.

Haskell — строго типизированный язык.
Вместо null используется
Maybe a — контейнер, который может содержать Nothing (аналог null)
Haskell — исключает null на уровне типов, делая его невозможным

---

## Типизация с выводом типа / Type inference

- механизм, при котором компилятор или интерпретатор автоматически определяет тип переменной или выражения на основе
контекста, без явного указания типа

- компилятор определяет тип по инициализатору - выражению справа от =
- определяет тип возвращаемого выражением значения
- устанавливает переменной тип, который определил по выражению
- является синтаксическим сахаром, сокращающим запись исходного кода
- не является реализацией динамической типизации

- применяется в Java для
  - дженериков
    - `List<String> names = List.of("Анна", "Борис");` - тип объектов списка не указан, компилятор проверяет по фактическому значению - в конструктор переданы строки, значит, список содержит строки
  - типа `var`
    - `var name = "user";` - компилятор определит для переменной тип String, исходя из типа значения ("user")  
  - лямбда-функций
    - `Function<String, Integer> parse = s -> Integer.parseInt(s);` - выражение Integer.parseInt принимает, поэтому тип String для параметра Function корректен
  - анонимных классов
    - 

---

## Семантический анализ
- этап проверки корректности исходного кода
- проверка основывается на логических и смысловых характеристиках кода:
  - соответствие фактических типов ожидаемым
  - наличие объявления переменной / поля класса, объявление предшествует вызову  
  - наличие объявления метода класса, объявление предшествует вызову  
  - допустимость операций 

### аналогия
как действует мозг, когда я вижу сообщение об ошибке - то есть, провожу бессознательный семантический анализ
- пытается найти причину глядя на сообщение об ошибке целиком
- избегает анализа, упрощает себе задачу, пытается решить напролом и быстро
- если решение устраняет причину, решение принимается
- по аналогии с типами такое решение я бы назвал "динамическим утиным"

---

# шаги типизации

действия компилятора javac по проверке типов

1. Синтаксический анализ
   - представление/парсинг исходного кода в виде абстрактного синтаксического дерева
   - проверка синтаксиса
   - если найдена синтаксическая ошибка - остановка компиляции
2. Создание таблицы символов
   - структура данных, содержащая связку "объект: имя, тип, адрес в памяти"
3. Проверка переменных
   - соответствие типа выражения типу переменной
4. Проверка методов
   - проверка типов переданных аргументов
   - типов возвращаемых значений
5. Проверка передачи null переменным примитивных типов 
6. Проверка дженериков
7. Проверка наследования
   - проверка типа оператором `instanceof`
8. Генерация байт-кода

---

# сила типизации

- типизация - процедура проверки типа объекта
- объекты проверки
  - переменные
    - `int a`
  - поля классов
    - `private final UUID id = UUID.randomUUID();`
  - параметры методов
    - `String[] args` в `public static void main(String[] args)`
  - выражения
    - `int` для `a * b`, где `int a`, `int b`
  - возвращаемые значения
    - `void` в `public static void main(String[] args)`

---

## [обзор свойств типизации в Java](../../src/main/java/DemoTyping/TypingExamples.java)

--- 

## 🔍 **Критерии оценки строгой типизации**:
1. **Строгая проверка типов во время компиляции**
2. **Неявные преобразования типов (автоматические приведения)**
3. **Поддержка типов-переменных (включая generic-типы)**
4. **Наличие систем типов с подтипами и полиморфизмом**
5. **Устойчивость к ошибкам типов (type safety)**
6. **Гибкость и читаемость кода при строгой типизации**
7. **Сложность изучения и использования**

---

### 📊 Сравнительная таблица по языкам программирования

| Язык           | Уровень типизации                                       | Примеры неявных преобразований                   | Генерики / об обобщённых типах | Поддержка подтипов (наследование)       | Ошибки типов на этапе компиляции | Комментарии                                                              |
|----------------|---------------------------------------------------------|--------------------------------------------------|--------------------------------|-----------------------------------------|----------------------------------|--------------------------------------------------------------------------|
| **Haskell**    | Очень строгая (статическая, сильная)                    | Нет (явные приведения)                           | Да (все типы обобщённые)       | Да (полиморфные типы)                   | Почти полностью                  | Идеальная типизация, но сложна для новичков.                             |
| **Rust**       | Строгая (статическая, сильная)                          | Очень ограничена (только через `as`)             | Да (с параметрами типа)        | Да (с наследованием trait-ов)           | Высокая                          | Отличная безопасность памяти, но сложный синтаксис.                      |
| **F#**         | Строгая (статическая, сильная)                          | Ограничена (явные приведения)                    | Да (обобщённые типы)           | Да (через интерфейсы и типы)            | Высокая                          | Хорошее сочетание функционального и императивного стиля.                 |
| **TypeScript** | Строгая (статическая, сильная)                          | Ограничена (но с `any` — можно обойти)           | Да (с поддержкой `generic`)    | Да (через интерфейсы и классы)          | Высокая (на этапе компиляции)    | Динамическая типизация в runtime, но строгая на этапе компиляции.        |
| **Java**       | Строгая (статическая, сильная)                          | Ограничена (примитивы → обёртки)                 | Да (generic-типы)              | Да (наследование классов и интерфейсов) | Высокая                          | Сильная типизация, но избыточна (шаблоны, дженерики).                    |
| **Kotlin**     | Строгая (статическая, сильная)                          | Ограничена (явные приведения)                    | Да (обобщённые типы)           | Да (наследование, interface)            | Высокая                          | Умеренно строгая, но с удобствами (null safety).                         |
| **Swift**      | Строгая (статическая, сильная)                          | Ограничена (явные приведения)                    | Да (generic-типы)              | Да (через protocol и class)             | Высокая                          | Хорошее сочетание типов и безопасного кода.                              |
| **Go**         | Строгая (статическая, сильная)                          | Нет (явные приведения)                           | Да (с ограничениями)           | Нет (интерфейс `any`)                   | Высокая                          | Простота и скорость, но отсутствие наследования.                         |
| **Python**     | Динамическая, но с поддержкой типов (myPy)              | Да (неявные приведения)                          | Да (с `type hints`)            | Да (через наследование)                 | Низкая (на этапе выполнения)     | Строгая типизация только с инструментами (myPy), но не в runtime.        |
| **JavaScript** | Динамическая (слабая)                                   | Да (неявные приведения)                          | Ограничено (с `any`)           | Да (наследование)                       | Очень низкая                     | Основная проблема — ошибки в runtime.                                    |
| **C++**        | Строгая (статическая, сильная)                          | Ограничена (явные `static_cast`, `dynamic_cast`) | Да (шаблоны)                   | Да (наследование)                       | Высокая                          | Мощная типизация, но сложна (шаблоны, `const_cast`, `reinterpret_cast`). |
| **Scala**      | Строгая (статическая, сильная)                          | Ограничена (явные приведения)                    | Да (паттерны, implicits)       | Да (наследование)                       | Высокая                          | Сложная типизация, но мощная.                                            |
| **Elixir**     | Динамическая, но с поддержкой типов (с помощью `@type`) | Да                                               | Ограничено                     | Нет (протоколы)                         | Низкая (на этапе выполнения)     | Сильная типизация только с инструментами.                                |


---

## 📈 Анализ по критериям

### 1. **Строгая проверка типов во время компиляции**
- Лучшие: **Haskell, Rust, F#, TypeScript (с mypy), Go**
- Средние: **Java, Kotlin, Swift, C++**
- Плохие: **Python, JavaScript, Elixir**

> ⚠️ Языки с **действительной статической типизацией** (без `any` или `dynamic`) — это те, где типы проверяются на этапе компиляции.

### 2. **Неявные преобразования типов**
- **Минимум**: Rust, Haskell, Go, F#
- **Средне**: Java, C++, Swift
- **Много**: JavaScript, Python (с `dynamic`)

> ⚠️ Чем больше неявных приведений — тем выше риск ошибок.

### 3. **Генерики / обобщённые типы**
- **Полная поддержка**: TypeScript, Java, C++, Go, F#, Rust
- **Ограниченная**: Python (с `type hints`), Elixir (с `@type`)
- **Отсутствуют**: JavaScript (без `any`)

> ✅ Хорошие языки — те, где генерики строгие и безопасные.

### 4. **Поддержка подтипов и наследования**
- **Полная**: Java, C++, Kotlin, Scala
- **Частичная**: Swift, F#, Go (через интерфейсы)
- **Ограниченная**: Rust (через trait-и, не наследование классов)

> ⚠️ Наследование — это не всегда плюс. Иногда — зло (нарушает инкапсуляцию).

### 5. **Ошибки типов на этапе компиляции**
- **Максимальная защита**: Haskell, Rust, F#, Go
- **Хорошая**: TypeScript, Java, Kotlin, Swift
- **Низкая**: JavaScript, Python (без инструментов)

> ✅ Язык с низкой вероятностью ошибок — это тот, где типы проверяются на этапе компиляции.

### 6. **Читаемость кода при строгой типизации**
- **Лучшая**: **Haskell, Rust, Go, TypeScript**
- **Средняя**: Java, C++, Kotlin
- **Хуже всего**: JavaScript, Python (без `mypy`)

> ✅ Хорошая типизация улучшает читаемость и документацию.

### 7. **Сложность изучения и использования**
- **Сложные**: Haskell, Rust, F#, Scala
- **Средние**: Java, C++, TypeScript
- **Простые**: Python, JavaScript

> ⚠️ Чем строже типизация — тем сложнее изучать, но тем безопаснее код.


---

# Реализация

## Java

