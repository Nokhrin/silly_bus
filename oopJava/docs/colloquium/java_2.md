Как тип данных связан с функциями (функии в широком смысле) и статической типизацией

определим понятие функции как элемента программирования и вычислений

функция - это операция над величинами и / или состояниями

строже
это математическое отображение из множества входных значений | параметров - область определения 
в множество выходных значений | возвращаемых результатов - область допустимых значений

функция обладает свойством детерминированности, то есть каждое вычисление над одними и теми же параметрами вернет один и тот же результат

функция в чистом виде зависит только от области определения
или
для каждого элемента в области определения существует один образ в области допустимых значений

в программировании функции могут быть неоднозначными | не детерминированными
такие функции изменяют состояние системы в виде побочного эффекта - изменяют переменные из нелокальных областей видимости
мошут зависеть от состояния - глобальных переменных, памяти
могут возвращать не детерминированные значения - например, случайные числа
могут зациклиться

различают чистые функции - соответствующие математическому определению
и процедуры - функции в понятиях прикладного программирования

тип данных определяет, какие значения могут быть параметрами функции, какие - результатами
при строгой типизации если функция принимает int , то передача функции значения иного типа вызовет ошибку

тип результата - в java и python - синоним типа функции
если функция возвращает int, то тип функции - int

тип данных - множество значений + допустимые операции

тип данных определяет возможные операции на величинами этого типа
операция - функция, определенная над типом

статическая типизация строго определяет допустимые операции и запрещает иные операции
статическая типизация - система проверки типов в compile time на основе формальных правил языка

то есть говорит "разрешено что определено"

---

Отличный запрос — вы подошли к теме с фундаментальной точки зрения, и теперь стоит уточнить и откорректировать ваше понимание строго, но конструктивно.

---

### 🔹 **Формальное определение функции (в контексте вычислений)**

**Функция** — это *отображение* из множества входных значений (область определения, или *domain*) в множество выходных значений (область значений, или *codomain*), обладающее свойством **однозначности**:  
> для каждого элемента из области определения существует **ровно один** образ в области значений.

Это — **математическое** определение. В программировании же мы сталкиваемся с **вычислительными функциями**, которые могут:

- зависеть не только от параметров, но и от **состояния** (например, глобальных переменных, памяти, времени);
- быть **недетерминированными** (например, генератор случайных чисел);
- не завершаться (зацикливаться);
- иметь **побочные эффекты** (изменять состояние системы).

Поэтому важно различать:

- **чистые функции** — соответствуют математическому определению: детерминированы, без побочных эффектов, всегда завершаются.
- **процедуры / императивные функции** — более общие конструкции, часто называемые «функциями» в языках вроде Java, но формально не являющиеся функциями в математическом смысле.

> ✅ **Уточнение**: когда вы говорите «функция — это математическое отображение», это верно **только для чистых функций**. В общем случае программистская «функция» — это *вычислительная процедура*, которая может не удовлетворять свойствам математической функции.

---

### 🔹 **Тип данных и его связь с функциями**

**Тип данных** — это формальное описание множества допустимых значений и операций над ними.

Более строго:
> Тип — это *множество значений* вместе с *интерфейсом допустимых операций* (включая функции, которые можно применять к этим значениям).

Связь с функциями:

1. **Тип определяет, какие значения могут быть аргументами и результатами функции.**
   - Например, если функция принимает `int`, то передать ей строку нельзя (в системе со статической типизацией).
2. **Функция сама имеет тип** — обычно записывается как `A → B`, где `A` — тип входа, `B` — тип выхода.
3. **Операции — это функции**, определённые на типах. Например, `+` — это функция типа `(Int, Int) → Int`.

> ✅ Ваше утверждение «тип данных определяет возможные операции» — **верно**, но стоит уточнить: тип определяет **сигнатуры** допустимых операций, а их реализация может быть внешней (например, методы класса, свободные функции и т.д.).

---

### 🔹 **Статическая типизация**

**Статическая типизация** — это система проверки типов **на этапе компиляции**, основанная на формальных правилах вывода типов.

Её цель — **гарантировать отсутствие определённого класса ошибок** (например, применение операции к несовместимому типу) **до выполнения программы**.

Ваша формулировка:  
> «статическая типизация строго определяет допустимые операции и запрещает иные операции — то есть говорит "разрешено то, что определено"»  

— **в целом верна**, но требует уточнения:

- Система типов **не описывает все возможные корректные программы**, а лишь **подмножество** тех, которые можно проверить статически.
- Она **ограничивает выразительность** ради безопасности: некоторые семантически корректные программы будут отклонены, если их типы не могут быть выведены.
- «Разрешено то, что определено» — это ближе к принципу **whitelisting**, но важно: **определено в рамках системы типов**, а не в абсолютном смысле.

> ⚠️ Пример: в Java нельзя написать функцию, возвращающую `String` в одной ветке и `Integer` в другой, если нет общего супертипа (кроме `Object`). Это ограничение системы типов, а не логической некорректности.

---

### 🔹 **Почему именно математическое отображение? Виды отображений**

Вы спрашиваете: *почему именно математическое отображение?*

Потому что **теория вычислений** (lambda-исчисление, теория категорий, денотационная семантика) строится на основе **математических функций** как идеального, предсказуемого и композируемого строительного блока.

#### Основные виды отображений (в математике):

| Тип отображения       | Свойство                                                                 |
|------------------------|--------------------------------------------------------------------------|
| **Инъекция**           | Разные элементы области определения → разные образы (`f(a) = f(b) ⇒ a = b`) |
| **Сюръекция**          | Каждый элемент области значений имеет прообраз (`∀y ∃x: f(x) = y`)         |
| **Биекция**            | Инъекция + сюръекция → взаимно однозначное соответствие                   |
| **Частичная функция**  | Не определена на всём множестве входов (например, деление на ноль)        |
| **Многозначная функция** | Одному входу сопоставляется несколько выходов (не функция в строгом смысле!) |

#### В программировании:

- **Чистые функции** моделируют **полные функции** (всюду определённые отображения).
- **Исключения, деление на ноль, зацикливание** → **частичные функции**.
- **Недетерминизм** (например, `random()`) → **не функции** в математическом смысле, а скорее **распределения вероятностей** или **операции в монадическом контексте**.

> 💡 Важно: даже если реальная функция в коде — не математическая, **мы стремимся моделировать её как таковую**, потому что это даёт:
> - композируемость,
> - возможность рассуждения (reasoning),
> - параллелизм без гонок,
> - тестирование без контекста.

---

### 🔸 Упражнения на осмысление и корректировку

1. **Приведите пример функции в Java, которая НЕ является математической функцией. Объясните, какое свойство нарушено.**
2. **Можно ли считать конструктор класса функцией? Если да — какова её область определения и область значений?**
3. **Почему функция `void foo(int x)` в Java не является функцией в математическом смысле? Что она возвращает?**
4. **Рассмотрите функцию `head : List<T> → T` (возвращает первый элемент). Является ли она всюду определённой? Как сделать её частичной/полной?**
5. **Если тип — это множество значений, то какой тип соответствует пустому множеству? А одноэлементному?**

---

### 🔸 Уточняющие наводящие вопросы (для коллоквиума)

- В каком смысле функция в программировании может **не быть** функцией в математическом смысле?
- Может ли система статической типизации **гарантировать чистоту функции** (отсутствие побочных эффектов)? Почему да/нет?
- Как тип результата функции связан с её **областью значений** в математическом определении?
- Если тип определяет допустимые операции, то можно ли сказать, что **функция — это часть определения типа**?
- В чём разница между **частичной функцией** и **функцией, возвращающей Optional**?

---

### 🔸 Как сформулировать вопрос на коллоквиуме (дружелюбно и чётко)

> «Я стараюсь понять, насколько корректно отождествлять программистскую функцию с математическим отображением. Могли бы вы помочь уточнить, в каких случаях это допустимо, а где возникает расхождение — особенно в контексте статической типизации?»

Или:

> «Если тип задаёт множество значений и допустимые операции, то можно ли рассматривать каждую функцию как “мост” между типами? И как система типов помогает гарантировать, что этот мост корректен?»

---

Готов углубиться в любую из этих тем — например, в связь с лямбда-исчислением, каррированием, полиморфизмом или алгебраическими типами данных.