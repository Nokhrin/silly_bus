## Вопросы

### приведение типов
какое техническое обоснование делать  
`FinCalc.Transfer transfer = (FinCalc.Transfer) op.getValue();`  
а не  
`FinCalc.Transfer transfer = op.getValue();`  - здесь уже объявлен тип, зачем еще кастить? Компилятор настолько не доверяет автору кода?
[пример в коде](../src/main/java/NoOop/OpsQueue.java#L57)  
что это - процедура/метод/оператор/особенность?
как это связано с компилятором - является его функциональностью?

### применение рефлексии
реализуя решение с рефлексией, встретил утверждение
что _рефлексия используется в Spring для `автозавязка бинов` (@Autowired, @Component)_
для меня это _бессвязный набор слов_ в настоящее время
надо ли на текущем этапе изучения разбираться в этом высказывании?

### учебный процесс и эффективность
что с твоей точки зрения я делаю неправильно?
что предпринять, чтобы исправить эти ошибки?

### почему это важно
почему необходимо понимать
устройство типизации?
работу компилятора?

> Типизация нужна что бы меньше косячить
> Понимание статитечской типизации и способов работы с рефлексией потребуется при работе с spring, т.к. это будет особого вида динамическая типизация

не достаточно ли писать **код, который работает**?  
почему важно понимать, **как** работает код, который работает?  
  
пример:  
1. есть класс А, реализованный программистом, понимающим компилятор  
2. класс А выполняет работу Р  
3. есть класс Б, реализованный программистом, НЕ понимающим компилятор  
4. класс Б выполняет работу Р  
  
существует ли преимущество класса А перед классом Б? (п.1 перед п.3 при условиях п2. и п.4)  
если существует, в чем оно заключается и проявляется?  

> В случае A - этим можно будет пользоваться и удобнее возможно, но точно не хуже
> с Б - под большим вопросом, практика показывает, что отсутствие типизации - оборачивается проблемами

### виды статической типизации
> у java своя глубина типизации, 
> у scala по больше будет, 
> type script по меньче чем scala, но больше чем java, 
> idris/coq больше чем предыдущие

статическая типизация - верхнеуровнего - способ гарантировать поведение системы?
статическая типизация - это не фиксированный набор правил?
что такое `глубина типизации`?
мое понимание - в языке есть статическая типизация / в языке нет статической типизации
что значит сравнение `типизация scala глубже типизации java`?
