https://chat.qwen.ai/s/f3e0182f-41b5-4a33-ab72-c83991b308b1?fev=0.2.10

## Связь EXE-файла и Оперативной Памяти (RAM)

EXE-файл: хранилище
v
Загрузчик (Loader): запись из exe в ram - команды в исполняемую область памяти, данные - в область чтения/записи

---

процедура преобразования исполняемого файла для выполнения - `load time`, 
имеет место перед выполнением - `runtime`


## указатель на функцию
```
void x() { логика X }
void y() { логика Y }
void (*runner)()    // указатель

runner = &x    // значение runner - ссылка на адрес памяти, с которого начинается код функции x
runner()    // вызов runner направит процессору адрес памяти, с которого начинается код функции x

```

почему не передать адрес x() напрямую?
для чего посредник - переменная указатель?
возможно, использование ссылки рационально при взаимодействии нескольких методов, для реализации полиморфизма - 
не меняя имя вызывающей функции возможно заменить выполняемый код с помощью замены указателя

функция из другого модуля
пример


точка входа программы A имеет начальный адрес в памяти START, который получает при начале выполнения (при помещении в исполняемую область памяти)?
адреса, используемые в программе A. относительные? 
то есть, являются смещением от START?


---

## память процесса

есть скомпилированная программа - код в машинночитаемой форме
при начале выполнения программы

ядро ос создает *экземпляр* программы - конкретный вычислительный процесс, конкретная работа по выполнению кода, для которой выделяются ресурсы системы
, аналогия - экземпляр класса есть конкретный объект

загрузчик читаем исполняемый пакет (например, exe) , создает в процессе **поток** по требованиям из пакета

поток состоит из исполняемого кода, данных в памяти, состояния, ресурсов (сетевые сокеты, файловые дескрипторы, отображения памяти), 

 

> ресурсы (сетевые сокеты, файловые дескрипторы, отображения памяти)- не есть данные?
> как ресурсы связаны с данными процесса?
> файловые дескрипторы - не в .bss? .data? в куче? почему?

процесс имеет id, 
**виртуальное адресное пространство** - диапазон адресов в ram, который разделяется на функциональные блоки - подпространства адресов:

пространство ядра
стек
куча
неинициализированные данные - .bss
инициализированные данные - .data
скомпилированный код - инструкции + константы - .text
пространство пользователя


> почему константы не в .data?


> пространство ядра?
зачем ядру пространство в памяти процесса? процесс иницализиуется ядром изначально, то есть ядро управляет процессом извне, для чего вводить пространство ядра в каждый процесс? это избыточно?



> пространство пользователя?
данные, код - результат деятельности пользователя, с какой целью еще пространство? резерв? если это резерв, когда может понадобиться? 



> `char *c` - указатель на значение типа char?
```
int main ()
{
  unsigned int x = 0x76543210;
  char *c = (char*) &x;  
    
    // (char*) &x - "читать char по адресу 0x76543210" ? 
    // указатель - это *c или &x ? что такое (char*) ?  
    // int* после имени типа?  - ожидаем ссылку на значение типа int?
    // *c - * до имени переменной - "ссылки, имя которой c"?
 
  if (*c == 0x10)
  {
    printf ("Underlying architecture is little endian. \n");
  }
  else
  {
     printf ("Underlying architecture is big endian. \n");
  }
 
  return 0;
}
```


неинициализированные данные / `.bss`
> пример - результат команды `int x;` ?

инициализированные данные / `.data`
> пример - результат команды `int x = 1;` ?

в .bss, .data - ссылка на значение? на тип значения?

в каком блоке адресного пространства окажется значение после выполнения ?
```
int x;
x = 1;
```

### вопросы 1

1. на каком этапе создан процесс?
2. какой объект инициализирует создание?
3. какой - создает?
4. какой выделяет область памяти для процесса?
5. что такое инициализация данных в случае информации, хранимой в сегменте стека .data?
6. что значит неинициализированные данные в .bss?
7. если стек и куча делят одно пространство адресов,
8. как реализовано различение, где закончился стек / началась куча и наоборот?


инициализация - 


### вопросы 2

```java
void my_func() {
    int a = 5;      // 'a' лежит в СТЕКЕ. Исчезнет после return.
    int b = a + 1;  // 'b' тоже в СТЕКЕ.
} // <- Здесь память под a и b освобождается автоматически!
```
1. `int a` инициализация - с каким объектом в памяти связана операция инициализации?
2. как отображается на память операция присваивания `=`?
3. какие действия, каким объектом в системном процессе будут выполнены для команды `int c = 2 + 3;` ?


```
Сравнение с Python:

    Python: def func(): x = 10. Имя x лежит в словаре локальной области видимости (который сам является объектом в куче).
    C: void func() { int x = 10; }. Значение 10 лежит непосредственно в ячейке стека. Никаких объектов, просто байты.
```
1. что значит "непосредственно в стеке" ?
2. в стеке **указатель на адрес памяти, с которого начинается значение** 
  или 
    **битовое представление значения**?
3. если представление значения, то как хранится информация о типе значения?
4. пример приведен для стека, но объекты python хранятся в куче, что этот пример показывает? используется ли стек для python кода? python работает с памятью напрямую? если код python компилится в c , то значение переменной python хранится в стеке?


---

сравнение абстракции java и абстракции python



---

## подитог

- константы в .text
- переменные функции - в stack 

---

## TODO

https://metanit.com/assembler/tutorial/1.1.php
главы 1,2,3,4,6,14

https://e-learning.bmstu.ru/iu6/pluginfile.php/2978/mod_data/content/1629/%D0%9E%D1%80%D0%B3%D0%B0%D0%BD%D0%B8%D0%B7%D0%B0%D1%86%D0%B8%D1%8F%20%20%D0%9E%D0%9F%20%D0%B8%20%D1%83%D0%BF%D1%80%D0%B0%D0%B2%D0%BB%D0%B5%D0%BD%D0%B8%D0%B5%20%D0%BF%D1%80%D0%BE%D1%86%D0%B5%D1%81%D1%81%D0%B0%D0%BC%D0%B8%20%D0%9E%D0%A1.pdf


