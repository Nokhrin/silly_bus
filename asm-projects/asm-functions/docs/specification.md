# Учебный проект `asm-functions` - 16 часов

## Цель

Понимание runtime-сред (JVM, CPython), отладка крашей, оптимизация вызовов.
call/ret для модульности кода, стек для локальных переменных, отладка в GDB backtrace

## Задача

Реализовать механизм вызова функций как в Python/Java с помощью стека x86-64.

## Тех. ограничения

- Linux x86-64, NASM синтаксис
- System V AMD64 ABI (аргументы в регистрах)
- Выравнивание стека 16 байт перед call
- Без libc (только syscall write/exit_group)

## План

## Спринт 1: Базовый вызов функции (6 часов)

| Шаг |                    Концепт                     | Часы |
|-----|------------------------------------------------|------|
| 1.1 | Инструкция call/ret                            | 1    |
| 1.2 | Кадр стека (push rbp / mov rbp, rsp / pop rbp) | 1    |
| 1.3 | Символы и метки (global, local, equ)           | 1    |
| 1.4 | Макросы препроцессора (%define, %undef)        | 1    |
| 1.5 | Передача аргументов (RDI, RSI, RDX)            | 1    |
| 1.6 | Возврат значения (RAX) + итоговая задача       | 2    |

---

## Спринт 2: Сохранение регистров (4 часа)

| Шаг |                         Концепт                         | Часы |
|-----|---------------------------------------------------------|------|
| 2.1 | Caller-saved регистры (RAX, RCX, RDX, RSI, RDI, R8-R11) | 1    |
| 2.2 | Callee-saved регистры (RBX, RBP, R12-R15)               | 1    |
| 2.3 | push/pop в прологе/эпилоге функции                      | 1    |
| 2.4 | Итоговая задача: функция сохраняет RBX/R12              | 1    |
| 2.5 | Двумерный массив: адресация, LEA, вложенные циклы       | 1    |

---

## Спринт 3: Рекурсия (6 часов)

| Шаг |                     Концепт                      | Часы |
|-----|--------------------------------------------------|------|
| 3.1 | Факториал: factorial(n) = n * factorial(n-1)     | 2    |
| 3.2 | Числа Фибоначчи: fib(n) = fib(n-1) + fib(n-2)    | 2    |
| 3.3 | Базовый случай, глубина рекурсии                 | 1    |
| 3.4 | Итоговая задача: GCD (Наибольший общий делитель) | 1    |

---

## Спринт 4: Вывод, отладка и I/O обертки (4 часа)

### Шаг 1: Конверсия integer->ASCII (Спринт 4.1)

|      Параметр       |                                                           Значение                                                            |
|---------------------|-------------------------------------------------------------------------------------------------------------------------------|
| Задача              | Реализовать функцию `int2ascii` для вывода чисел в stdout                                                                     |
| Концепты            | Деление на 10, остаток, конверсия цифр в ASCII, буфер в .bss                                                                  |
| Ожидаемый результат | `./step4-1_int2ascii` -> есть вывод в терминал, `./step4-1_int2ascii > output.txt` -> есть вывод в файл, нет вывода в консоль |
| Время план          | 2                                                                                                                             |
| Время факт          | 2.5                                                                                                                           |

- Деление на 10 дает цифры в обратном порядке (младшая первая)
- ASCII цифр: '0'=0x30, '1'=0x31, ..., '9'=0x39
- Буфер заполняется с конца, вывод с начала

---

### Шаг 2: I/O обертки (Спринт 4.2)

|        Параметр         |                                                                                             Значение                                                                                             |
|-------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Название                | Процедуры ввода-вывода с управлением кадрами стека                                                                                                                                               |
| Ограничение на регистры | Использовать не более 3 регистров общего назначения для вычислений. Параметры и локальные переменные размещать только в стеке. Регистры RDI/RSI/RDX/RAX загружать исключительно перед `syscall`. |
| Соглашение о вызовах    | Параметры передаются через стек (push перед call или размещение в кадре вызывающей функции). Доступ через `[RBP + 16]`, `[RBP + 24]` и т.д. Локальные данные по `[RBP - offset]`.                |
| Ожидаемый результат     | Программа читает строку из stdin, выводит её, конвертирует число 3628800 в ASCII, выводит в stdout. Код выхода = 0. Стек сбалансирован после каждого вызова.                                     |
| Время план              | 2                                                                                                                                                                                                |
| Время факт              | 12                                                                                                                                                                                               |

---

### Шаг 3: GDB backtrace и stack frames (Спринт 4.3)

|      Параметр       |                                                                 Значение                                                                 |
|---------------------|------------------------------------------------------------------------------------------------------------------------------------------|
| Задача              | Разделение I/O и рекурсии в разные объектные файлы тренирует extern/global, линковку нескольких файлов и отладку сломанных кадров стека. |
| Концепты            | `bt`, `info frame`, `x/8gx $rsp`, анализ краша                                                                                           |
| Ожидаемый результат | В GDB найдена и продемонстрирована ошибка структуры кадра стека                                                                          |
| Время план          | 2                                                                                                                                        |
| Время факт          | 3                                                                                                                                        |

---

