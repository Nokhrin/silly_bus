:# Изучение формальной логики и математических основ программирования

## Цель
Практическое освоение:
- Логики высказываний и таблиц истинности
- Формальных методов верификации программ
- Математической строгости в разработке ПО

## Источники

## Структура
"The Science of Programming" (David Gries, 1981)
[логика высказываний](science_of_programming_gries/ch_1.md) - решения упражнений главы 1



## Символы логических операторов (по Gries)

| Символ | Название                | Приоритет | Аналог в коде      |
|--------|-------------------------|-----------|--------------------|
| `¬`    | отрицание (not)         | высший    | `!` / `not`        |
| ``     | конъюнкция (and)        | высокий   | `&&` / `and`       |
| `∨`    | дизъюнкция (or)         | средний   | `\|\|` / `or`      |
| `⇒`    | импликация (implies)    | низкий    | `a ⇒ b` ≡ `¬a ∨ b` |
| `⇐`    | обратная импликация     | низкий    | `a ⇐ b` ≡ `b ⇒ a`  |
| `≡`    | эквивалентность (iff)   | низший    | `==` для bool      |
| `≠`    | неэквивалентность (xor) | низший    | `!=` для bool      |

## Печать таблицы истинности
```shell
python3 science_of_programming_gries/truth_table.py '((not p) or q) and (not(not p) or r)'
```